# 진행 이력 (progress log)

이 파일은 세션 간 컨텍스트 유지를 위한 짧은 이력 노트입니다. 매 세션이 끝날 때 한 줄씩 남기세요.

형식 예시:

```
- 2026-04-23 아이디어 검증(/validate-idea) 완료. 판정: 🟡 피봇. 가격 모델 재검토 필요.
- 2026-04-24 린캔버스 1차 작성. UVP 문구 재고 필요.
```

---

- (템플릿 초기화) 새 프로젝트 시작
- 2026-04-24 CLAUDE.md 템플릿 플레이스홀더 7개 치환 완료 (프로젝트: physical_analysis, 타겟: 한국, 단계: 아이디어 발굴/검증)
- 2026-04-24 Kubios HRV Scientific 기능/지표/시장 조사 완료 (40+ 지표, 탭 구조, 라이선스)
- 2026-04-24 NIRS-VOT 분석 엔진 Python 포팅 완료 (scripts/nirs_vot/). R 스크립트 12지표 1:1 재현. 인코딩·구분자 자동 감지로 NOR/HYPO/HYPER 이질적 포맷 모두 처리. 6세션 전체 실행·시각화 검증 완료 → docs/결과-results/
- 2026-04-24 Windows Python 3.10(NumPy 1.24)에서도 동일 재현 검증. trapezoid/trapz shim + stdout UTF-8 재설정으로 크로스플랫폼 완성. WSL↔Windows 수치 차이 최대 6.9e-16(기계 엡실론). → docs/결과-results-windows/
- 2026-04-24 Kubios UI/UX 분석 (로컬 7장 + 웹 2차 자료) 기반으로 GUI 설계 문서 작성 → docs/GUI설계-gui-design.md
- 2026-04-24 GUI v0.1 구현 완료 (PySide6 6.11 / pyqtgraph 0.14). 3분할 레이아웃(Data Browser + Tabs + Parameter Table), 메뉴바 5개(File/Edit/View/Tools/Help), NIRS-VOT 전용 탭 + 5개 Kubios 탭 Placeholder. Windows/WSL 양쪽에서 실행 검증. CJK 폰트 자동 감지(Malgun Gothic/Noto Sans CJK KR). → scripts/nirs_vot_gui/
- 2026-04-25 **중대 방향 재조정**: "Kubios 클론"은 UI 껍데기가 아니라 HRV 기능 자체를 구현해야 했음. Kubios 공식 User's Guide PDF(48p) + 이찬민_NOR_HRV 리포트 CSV/PDF 전수 파싱. → scripts/hrv_analysis/ 엔진 구현 (Time/Freq/Nonlinear, Tarvainen smoothness priors detrending, Welch PSD 4Hz interp+300s window, SD1/SD2, ApEn/SampEn, DFA α1/α2). 29개 지표 중 20개 <3% 오차, 9개 <15%. 핵심 임상 지표(RMSSD, LF/HF, SD1, SampEn, DFA α1) 모두 1% 이내. GUI에 HRV Overview 탭 통합, Ours vs Kubios 비교 테이블 자동 표시. → docs/hrv-검증리포트-validation.md
- 2026-04-25 GUI v0.3 재배치: HrvController(공유 분석 상태) 도입, 자동 로드 제거(File→Open으로만 로드), 4개 HRV 탭(Overview/Time-domain/Frequency-domain/Nonlinear) 큰 그래프 전용 뷰로 신규 구현. 우측 도크에 CompareTable(36개 지표 색상 코딩) — 활성 탭에 따라 카테고리 자동 필터. NIRS-VOT 탭은 별도 보너스로 유지(우측 패널 자동 전환). → docs/결과-results/gui-screenshots-v0.3/
- 2026-04-25 v0.3.1: 좌측 도크도 탭 따라 자동 전환. HRV 탭에서는 HrvDataBrowser(파일 메타·sample 정보·beats), NIRS-VOT 탭에서는 기존 세션 트리. 좌우 도크 모두 일관된 컨텍스트 전환.
- 2026-04-25 v0.4: 실제 Kubios 스크린샷과 정렬되도록 메인 레이아웃 재구성. 상단 RrSignalView(전체 측정 + 선택 sample RR, 항상 보임)와 하단 결과 탭(Kubios 명칭: Results overview / Time-Domain / Frequency-Domain / Nonlinear / Time-Varying)을 QSplitter(Vertical)로 분할. 각 결과 탭은 Kubios 패턴(좌 통계표 + 우 그래프)으로 재구현. Overview는 색상 타일 16개 + 미니 PSD/Poincaré. 메뉴바 Reports 추가(File/Edit/Reports/View/Tools/Help). NIRS-VOT 탭 활성 시 상단 시그널 자동 숨김.
- 2026-04-25 v0.4.1: 그래프 뭉개짐/강제 고정 문제 해결. MplCanvas에 show_toolbar 옵션 추가하고 HRV 결과 탭의 모든 캔버스에서 NavigationToolbar 제거(Kubios 스타일). _AdaptiveCanvas로 sizeHint를 작게(200x120) 잡아 splitter가 자유롭게 줄임. setSizePolicy(Expanding, Expanding). 1280x800 / 1920x1080 두 화면 크기에서 그래프 적응 검증. → docs/결과-results/gui-screenshots-v0.4.1/
- 2026-04-25 v0.4.2: 데이터 로드 후 상단 두 그래프가 collapse 되는 문제 수정. matplotlib의 'axes sizes collapsed to zero' 경고가 원인이었음 — figsize 너무 작아 axis label·title 마진 빼면 plot 영역 0이 됨. 해결: figsize (10, 2.4)/(10, 3.0)로 확대, 캔버스별 setMinimumHeight, RrSignalView 자체 minimumHeight=320, main_splitter 기본 비율을 상단 우선([400, 480])으로. 1280/1600/1920 세 크기에서 검증. → docs/결과-results/gui-screenshots-v0.4.2/
- 2026-04-25 v0.4.3: 해상도·DPI 무관 레이아웃. sizing.py 헬퍼 신설(lh/char_w/screen_size/scaled_size/dock_width/mpl_dpi). Qt6 HighDpiScaleFactorRoundingPolicy.PassThrough 명시. 모든 고정 px(setMinimumHeight, setMinimumWidth, setHandleWidth)를 폰트 라인 높이 배수로 변환. MainWindow 초기 크기는 화면 가용 영역 0.85배. resizeEvent에서 splitter 비율(4:5) 자동 유지(사용자가 핸들 드래그 전까지). matplotlib Figure DPI를 화면 logical DPI에 동기화(96~120 클램프). 1366/1600/1920/2560/3840 5개 해상도에서 검증, 모두 4:5 비율과 컴포넌트 배치 일관 유지. → docs/결과-results/gui-screenshots-v0.4.3/
- 2026-04-25 v0.5: 시간 범위 선택 + 시간축 토글 기능 추가. HrvController.analyze_range(t0, t1) 도입(절대 초 기준 RR 필터링 후 재분석, 최소 20 beats). RrSignalView에 (1) Measurement overview에서 SpanSelector 마우스 드래그로 범위 선택, (2) HH:MM:SS 입력 박스 + Apply/Reset 버튼, (3) Time axis 토글(Relative/Absolute clock). controller.range_changed 시그널이 모든 탭(Overview/Time/Freq/Nonlinear) 자동 갱신. 상태바에 [FULL]/[RANGE] 태그 + 시작·종료 표시. 검증: 전체 SDNN 23.73 → 60-180s 24.77 → 30-150s 22.63으로 실제로 다른 구간 분석됨. → docs/결과-results/gui-screenshots-v0.5/
- 2026-04-30 **v0.6 데이터 모델 재정의**: 측정 한 세션 = (`{이름}_{cond}_Calf.{txt,csv,xlsx}` 시계열) + (`{이름}_{cond}.xlsx` 4시트 설정 파일) 페어. (1) `nirs_vot/loader.py`에 `.csv` 추가, `load_calf` 통합 함수로 .txt/.csv/.xlsx 단일 진입점. (2) 신규 `scripts/config_loader.py` — Info,GXT/Timepoint/SpO2,BP,TQR,La/Coded Data 4시트 파싱. `vot_truth`·`hrv_truth` ground truth 추출. (3) `nirs_vot/anchors.py`에 `anchors_from_session_config()` 동적 산출 (Start(Occ)→inflate, Fin(Def)→deflate, ±lead/tail). 하드코딩 SESSION_ANCHORS는 fallback. 3 condition × 2 세션 모두 하드코딩과 일치 검증. (4) GUI: File 메뉴 재구성 — Open _Calf data + Open measurement folder, DataBrowser는 폴더 스캔(`discover_sessions`) 기반 동적 구성, NirsVotTab은 자동 페어링 + condition 추정. (5) ParameterTable에 ground truth + err% 컬럼 추가 — NIRS-VOT 결과를 Coded Data의 VOT_*/Calf_SmO2_* 와 자동 비교 (Slope1_30_150·Base/Min/Peak·AUC_3min 모두 <5%). (6) 신규 `widgets/external_hrv_card.py` — _Calf에 RR 없으므로 (α) 가정으로 HRV 탭은 외부 결과 카드 뷰(Coded Data의 HRV_SDNN/RMSSD/LF/HF/LF·HF, Baseline+Post 두 컬럼). HRV 분석 엔진(scripts/hrv_analysis/)은 보존하되 Kubios CSV 로드 시에만 활성화 — β/γ 옵션 확장 가능. → docs/데이터모델-data-model.md
- 2026-04-30 v0.6.1 페이지 분리 + Windows 실행 스크립트. (1) 최상위 탭을 `🩸 NIRS 분석` / `❤ HRV 분석` 두 페이지로 분리 — 한 화면에 6개 탭이 섞여 복잡하던 구조를 모드 단위로 정리. (2) 신규 `tabs/nirs_analysis_page.py` (NirsVotTab + 폴더 선택 toolbar), `tabs/hrv_analysis_page.py` (Open Kubios CSV 버튼 + RrSignalView + 5 sub-tab 자체 보유). 각 페이지가 자체 load 버튼을 가지므로 File 메뉴에서 Load 액션 제거, View 메뉴에 Ctrl+1/Ctrl+2 페이지 전환 단축키 추가. (3) 좌·우 도크는 활성 페이지에 따라 자동 콘텐츠 전환 (NIRS=DataBrowser+ParameterTable, HRV=HrvDataBrowser+ExternalHrvCard/CompareTable). (4) Windows 실행 스크립트: `setup.bat` (`.venv-win` 생성 + requirements 설치), `run.bat` (PYTHONUTF8=1 + cwd 고정), `requirements-win.txt` (PySide6/matplotlib/scipy/pandas/numpy/openpyxl). PowerShell `.\` 누락 해결을 위해 cmd 호환 .bat 우선.
- 2026-04-30 v0.6.2 자동 로드 제거 + 설정 파라미터 편집 패널. (1) NirsVotTab 시작 시 `_load_all_defaults` 자동 호출 제거 — 사용자가 [Open data file] 또는 [Load all default files] 로 명시적 로드. analyze_session 의 fallback 자동 로드도 제거 (미로드면 경고 후 중단). (2) 신규 `widgets/parameter_panel.py` — Subject info 12 필드(read-only) + NIRS_VOT/HRV Timepoint 8 QTimeEdit + lead/tail 2 QSpinBox + Reset/Apply 버튼. 설정 xlsx 로드 시 모든 input box 가 자동으로 채워짐. (3) 사용자가 시각·lead/tail 수정 후 Apply → SessionConfig in-memory 갱신 + LoadedFile.dynamic_anchors 재산출 + 자동 재분석. 원본 xlsx 는 건드리지 않음. (4) condition 콤보 전환 시 패널이 해당 condition 의 값으로 자동 갱신, 미로드 condition 이면 클리어 + Apply disabled. 검증: HYPER 로드→14:01-14:06 자동 표시, Baseline start 14:00:30/lead 30s 로 변경 후 Apply→ dynamic_anchor=(14:00:00, 14:00:30, 14:05:30, 14:07:30) 정확히 반영, NOR 콤보 전환→13:03:00 자동 갱신.
- 2026-04-30 v0.6.3 그래프 우선 + 미리 박힌 데이터 완전 제거 + 전 필드 편집 가능. (1) ParameterPanel 을 NirsVotTab 본문에서 떼어 별도 `QDockWidget` (Session Parameters) 로 이동. 기본 hidden, Ctrl+P 또는 toolbar/View 메뉴에서 토글. dock_params 와 dock_right 를 tabify 해서 우측 영역 공유. visibilityChanged → act_toggle_params 자동 동기화. (2) "이찬민" / `참고자료/NIRS_VOT(SmO2) 데이터` 자동 스캔 모두 제거. DataBrowser 시작 상태는 "(no measurement folder loaded)", subject="" 빈 값. NirsVotTab 의 `_default_calf_for("이찬민_…")` 함수와 "Load all default files" 버튼 제거. 폴더 일괄 로드는 임의 폴더의 `*_Calf.{txt,csv,xlsx}` 를 condition 자동 추정으로 처리. 사용자가 [Open folder] 호출하기 전까지 어떤 데이터도 보이지 않음. (3) ParameterPanel 모든 필드 read-only QLabel → 편집 가능 input. Subject info 18 필드 (ID/Name/D.O.B/Age/Gender/Weight/Height/Wattmax/VO2max/W·kg/FTP/AT/RC/Wattmax 90·40·110%/안장 높이/안정시 HR) 가 QLineEdit / QDoubleSpinBox / QSpinBox / QComboBox / QDateEdit 로 모두 편집 가능. Apply 시 SessionConfig.subject_info 도 in-memory 갱신. (4) Close 버튼 추가 — 모든 로드된 데이터·결과·패널 한 번에 클리어. (5) 좌측 트리에서 condition 노드 더블클릭 → 그 페어 자동 로드. 검증: 시작 시 빈 상태 / HYPER 로드→18 input 자동 채움(Wattmax 370·VO2max 67.1·Saddle 8.2 등) / Wattmax 380·Name TestSubject 직접 수정 후 Apply→ cfg.subject_info 갱신 + dynamic_anchors=(13:59:45, 14:00:30, 14:05:30, 14:08:00) (lead 45s/tail 150s 반영) / Close→ 모든 패널 클리어 / load_folder→ 6 sessions / 도크 토글 정상.
- 2026-04-30 v0.6.4 데이터 진입점·파라미터 진입점·토글 패널 위치 재배치 (대시보드 표준 정렬). (1) **Load 버튼 → 좌측 Sessions 패널**: NirsVotTab 본문 상단의 `📂 Open data file…` / `📁 Open folder…` 두 버튼을 제거하고 `widgets/data_browser.py` (DataBrowser) 헤더로 이동. DataBrowser 를 `QTreeView` 단독에서 `QWidget(QVBoxLayout(헤더 + QTreeView))` 로 재구성. 새 시그널 `open_file_clicked` / `open_folder_clicked` 만 emit, 실제 다이얼로그 처리는 NirsVotTab 의 기존 `_on_open_file` / `_on_open_folder` 가 그대로 담당. app.py `_wire_signals` 에서 시그널 → 핸들러 와이어링. NirsVotTab 본문 상단은 Condition / Session / Analyze / Close 만 남겨 그래프 시야 확보. (2) **Parameter 로드 버튼 → ParameterPanel 헤더**: 외부 SessionConfig xlsx 파일을 측정 시계열과 무관하게 단독 불러올 수 있는 `📂 Load preset…` 버튼 추가. `_on_load_preset` 메서드가 QFileDialog → `load_session_config(path)` → `show_for(cfg)` 로 모든 입력 위젯을 채움. 타이틀 라벨에 "preset, not bound to measurement" 표기. Apply 자동 호출 안 함 (사용자 검토 후 수동). (3) **Session Parameters 도크 → 상단 (TopDockWidgetArea)**: 기존 우측 영역(Results 와 `tabifyDockWidget`로 묶임) → `Qt.TopDockWidgetArea` (메뉴바 아래·메인 캔버스 위) 로 이동. tabifyDockWidget·dock_right.raise_() 호출 제거. 토글 시 가로 슬라이드 다운 패턴 → 가시성 ↑, 그래프 좌우 폭 영향 0. visibilityChanged → 메뉴 토글 동기화 그대로. (4) info_label 안내 문구 갱신: "좌측 Sessions 패널 상단의 [📂 Open data file…] / [📁 Open folder…] 버튼을 사용하세요". (5) APP_TITLE v0.6.3 → v0.6.4. PySide6 6.11.0 + 헤드리스 (QT_QPA_PLATFORM=offscreen) 임포트·시그널·메서드 검증 통과 — DataBrowser MRO=[QWidget,QObject], ParameterPanel._on_load_preset 존재, DataBrowser.open_file_clicked 시그널 존재.
- 2026-04-30 v0.6.5 Session Parameters 도크 우측 복귀 + 탭 라벨 상단 배치. v0.6.4 의 Top 영역 배치를 사용자 피드백으로 되돌림. (1) `addDockWidget(TopDockWidgetArea, dock_params)` → `addDockWidget(RightDockWidgetArea, dock_params)`. (2) `tabifyDockWidget(dock_right, dock_params)` 다시 추가 — 우측 도크 영역에 Results / Session Parameters 두 도크를 한 영역에 탭 묶음. (3) **`setTabPosition(RightDockWidgetArea, QTabWidget.North)`** — 우측 도크 영역의 탭 라벨이 도크 영역 **상단**에 위치 (Qt 기본값 South → North). 사용자는 우측 도크 위쪽에서 두 탭 라벨(`NIRS-VOT · Ours vs ground truth` / `Session Parameters`)을 클릭으로 전환. (4) `dock_right.raise_()` 로 시작 시 Results 가 활성. dock_params 는 시작 hidden, Ctrl+P 토글로 켤 때 같은 영역에 탭으로 합류. visibilityChanged → 메뉴 토글 동기화 그대로. 검증 (offscreen): dock_right area=Right, dock_params area=Right, tabPosition(Right)=North, 양쪽 visible 시 tabifiedDockWidgets 양방향 매칭, 토글 시 Results 는 항상 visible / Parameters 만 토글.
- 2026-04-30 v0.6.6 NIRS-VOT 그래프 위 핸들 5종 직접 드래그 조절. (1) `metrics.py` `compute_metrics` 에 `override_baseline` / `override_min_time` / `override_peak_time` 키워드 추가. Min/Peak time override 시 그 시점의 SmO2_smooth 보간으로 y 자동 결정. magnitude·T50·T95·oxy_deficit·AUC 모두 새 값 기준 재계산. (2) 신규 `widgets/draggable_overlay.py` — Axes 위 axvline / axhline / scatter 핸들 통합 매니저. mpl_connect 으로 button_press/motion/release 처리. pixel-space hit-test (radius 6px) 로 가까운 핸들 감지. NavigationToolbar zoom/pan 모드일 때 자동 차단. drag-end 시 콜백(kind, name, value). (3) `LoadedFile.overrides: dict[str, dict]` 필드 추가 — session 별 (baseline / min_time / peak_time) 사용자 지정 값 저장. analyze_session 이 자동으로 override 인자 전달. (4) `NirsVotTab._render` 가 baseline/start/inflate/deflate/end/Min/Peak 7개를 모두 DraggableOverlay 로 등록. drag-end → `anchor_dragged(kind, name, value)` 시그널. (5) `app.py._on_anchor_dragged` 가 시그널 수신 → vline 'i'/'d' → cfg.timepoints['NIRS_VOT'][session] 갱신, vline 's'/'e' → ParameterPanel.lead_spin/tail_spin 갱신, hline → overrides[baseline], marker → overrides[min_time/peak_time]. 모두 ParameterPanel 동기화 + analyze_session 자동 재호출. 검증: baseline 70 드래그→재분석 baseline=70.00 / Min 14:04:00 드래그→자동 보간 28.89% / Peak 14:06:20 드래그→72.89% / inflate 14:01:30 드래그→cfg.NIRS_VOT.start=14:01:30 / start 14:00:30 드래그→lead_spin=60s / end +210s 드래그→tail_spin=210s, dynamic_anchors.end=14:09:30. 모든 케이스에서 ParameterPanel 자동 갱신.
- 2026-04-30 v0.6.6.1 드래그 동작 안 되던 두 버그 + matplotlib 한글 깨짐 수정. (1) **`DraggableOverlay._toolbar_active()` AttributeError 픽스**: Qt 임베디드 캔버스에선 `canvas.manager` 가 None 이라 `manager.toolbar` 호출이 죽고, 그 결과 `_on_press` 의 첫 검사에서 예외 발생 → hit-test 가 영원히 실행되지 않음. canvas/manager/toolbar 단계마다 `getattr(..., None)` 로 안전 분기. (2) **mpl_connect 누수 픽스**: `fig.clear()` 가 mpl_connect 콜백을 정리하지 않아 재분석마다 옛 overlay 의 stale ax 참조 callback 이 누적. `DraggableOverlay.disconnect()` 메서드 추가 + `NirsVotTab._render` 시작 시 옛 overlay 의 mpl_disconnect 호출. (3) **marker pick_radius 6→10px** — 클릭 hit 영역 확대. (4) **matplotlib 한글 폰트 설정** — `main._apply_matplotlib_cjk` 신설. PySide6 와 matplotlib 은 폰트 검색 시스템이 별개. Linux 의 `Noto Sans CJK*.ttc` 는 `findSystemFonts(fontext='ttf')` 로 안 잡혀 글리프 깨짐 발생. 해결: `fc-list :lang=ko --format=%{file}` 로 한글 폰트 파일 직접 발견 후 `fontManager.addfont(p)` 등록 + `font.sans-serif` 우선순위 리스트에 등록된 CJK face (예: ttc 첫 face=Noto Sans CJK JP — 한글 글리프 동일하게 포함) 자동 추가. axes.unicode_minus=False. 검증: 실제 mouse event (button_press/motion/release) 시뮬레이션으로 baseline·inflate·Min·start 모두 hit-test 통과 + override 저장 + ParameterPanel 동기화 + 재분석 모두 작동. 한글 텍스트(`'한글 테스트 — 드래그'`) 글리프 경고 없이 렌더링.
- 2026-04-30 v0.6.7 드래그 실시간 fill·Parameter 동기화 + 성능 최적화. (1) **`fig.clear()` 제거**: `NirsVotTab._render` 를 `_setup_axes` (1회 + condition 첫 호출 시) + `_update_render` (매 호출, set_data 만) 로 분리. raw·smoothed Line2D, vlines/hline/markers (DraggableOverlay 핸들), text 라벨 모두 setup 시 한 번 만들고 update 마다 위치만 갱신. fill_between (PolyCollection) 만 매번 remove + 재생성. autofmt_xdate 도 setup 시 1회. → 100 회 재분석 4.85s (48.5 ms/회) 로 측정. (2) **`DraggableOverlay.update_handle(kind, name, *args)`** — artist 의 set_xdata/ydata/offsets 만 호출하는 빠른 경로. fig.clear 후 새로 만들 필요 없음. (3) **`DraggableOverlay` 에 `on_drag_motion` 콜백** 추가. mpl motion_notify 마다 호출. (4) **`NirsVotTab._on_overlay_drag_motion`** 이 motion 마다 현재 핸들 위치(s/i/d/e/baseline) 를 직접 읽어 `_rebuild_fills` 로 Hyperemia/Oxygen Deficit 영역 즉시 다시 그림 → 사용자가 baseline·deflate 끌면 색칠 영역이 실시간으로 따라옴. (5) **`anchor_dragging(kind, name, value)` 시그널** 추가. `app._on_anchor_dragging` 이 motion 중에도 ParameterPanel 의 NIRS_VOT timepoint QTimeEdit / lead_spin / tail_spin 만 가볍게 직접 갱신 (`show_for(cfg)` 같은 무거운 호출 회피). (6) `clear_all` 이 _ax / line / fill / overlay state 모두 리셋. 검증 (offscreen): inflate +30s drag → param input 14:01:30 즉시 표시, +60s → 14:02:00; baseline 70 drag → motion 마다 fill_deficit 재생성 확인; start +30s drag → lead_spin 30s 즉시, +50s → 10s; release 시 모든 값 cfg/dynamic_anchors/overrides 에 commit + 12 row ParameterTable 갱신.
- 2026-04-30 v0.6.8 SVG 아이콘 5종 + Sessions 트리 자동 반영. (1) **신규 `assets/icons/`** — `data_tree.svg` (폴더+가지 트리), `results.svg` (4셀 테이블), `params.svg` (3-슬라이더), `nirs.svg` (SmO2 VOT 곡선 + baseline 점선, 빨강), `hrv.svg` (ECG QRS 파형, 파랑). 모두 24×24 viewBox, stroke-only line-art. PySide6 QIcon 이 SVG 직접 로드. (2) **`app.py` 에 `_icon(name)` 헬퍼** 추가. (3) **toolbar 가시성 ↑** — `act_toggle_browser` ("Toggle Sessions Browser") / `act_toggle_results` / `act_toggle_params` 모두 setIcon 적용 + setToolTip 추가. toolbar `setIconSize(20×20)` + `ToolButtonTextBesideIcon` 으로 아이콘 + 텍스트 함께 표시. View 메뉴 항목에도 동일 아이콘. (4) **페이지 탭 아이콘** — `pages.addTab(_icon('nirs'), ...)` / `_icon('hrv')` 으로 NIRS/HRV 분석 탭 헤더에 도메인 아이콘. (5) **Open 한 파일이 Sessions 트리에 자동 반영 (버그 픽스)** — 이전엔 `📂 Open data file…` / `📁 Open folder…` 로 로드해도 좌측 Sessions 트리는 폴더 스캔 기반이라 갱신 안 됨. `app._on_measurement_loaded` 가 `nirs_browser.set_folder(loaded_file.path.parent)` 호출하도록 수정 → 단일 파일 로드해도 그 파일이 있는 폴더의 모든 (config xlsx + _Calf) 페어가 즉시 트리에 등장. 검증: HYPER 단일 파일 로드 → 트리에 NIRS_VOT(SmO2) 데이터/ HYPER + HYPO + NOR 3 condition × 2 session 자동 표시; toolbar/탭 아이콘 모두 isNull=False.
- 2026-04-30 v0.6.9 Sessions 트리: 로드 실제 항목만 표시 (v0.6.8 자동 폴더 스캔 이슈 수정). 사용자 피드백 — "로드되지 않았는데 Session 에 반영되는 건 이상함". 폴더 안에 _Calf 파일이 있다는 사실만으로 트리에 자동 표시되던 v0.6.8 동작은 의도와 어긋남 (실제 메모리에 로드된 게 아닌데 보임). (1) **`DataBrowser` 재구성** — `widgets/data_browser.py` 에서 `discover_sessions` 폴더 스캔 흐름 제거. `subject="" / folder=None` 파라미터 제거. 새 메서드 `set_loaded(loaded_dict)` — `NirsVotTab.loaded` (dict[str, LoadedFile]) 를 받아 트리 빌드. 시작 시 + clear 시 placeholder ("(no data loaded) — Open data file… / Open folder…"). 새 시그널 `condition_selected(condition)` (이전 `measurement_picked` 대체). (2) **subject 자동 추출** — 트리 루트 라벨은 첫 LoadedFile 의 `cfg.subject_info['Name']` 또는 `ID(Protocol)` 에서. 없으면 "Subject". (3) **app.py wiring** — `_on_measurement_loaded` 가 `nirs_browser.set_loaded(self.tab_nirs.loaded)` 호출 (v0.6.8 의 set_folder 호출 제거). `_on_nirs_cleared` 가 `nirs_browser.clear()` 추가. `_on_browser_condition_selected` 핸들러 — condition 노드 더블클릭 → 콤보 setCurrentText (loaded 에 있으면). (4) **condition 정렬** — NOR < HYPO < HYPER 우선순위 (의학적 진행 순서). 검증: 시작 비어있음→placeholder, HYPER 1개 로드→트리에 HYPER 1개만, NOR 추가→2개, Close→비움+placeholder, load_folder→3개, HYPO 노드 더블클릭→condition_combo HYPO 전환.
- 2026-05-01 v0.6.10 NIRS 페이지에 [전체 데이터] sub-tab 추가 (구간 잡기 참고용). NIRS 분석 페이지가 `NirsVotTab` 단독 → `QTabWidget` 컨테이너로 재구성. (1) 신규 `tabs/full_trace_tab.py` (`FullTraceTab`) — 로드된 condition 의 전체 측정 시계열을 한 화면에 표시. 상단 컨트롤: condition combo + 토글(Raw 곡선 / NIRS_VOT 구간 음영 / 운동 구간 음영 / THb twin axis). axvspan 으로 Baseline (파랑)·Recovery2 (주황)·Ex1·Ex2 (빨강) 영역을 시각화 → 사용자가 어디가 어느 구간인지 한눈에 파악, ParameterPanel 또는 분석 그래프 드래그로 구간을 잡을 때 참조. (2) `NirsVotTab.loaded` 와 동기화: `measurement_loaded`/`measurement_cleared` 시그널 받아 콤보 옵션 자동 갱신, `condition_combo.currentTextChanged` 시 본 탭 콤보도 따라감. (3) `tabs/nirs_analysis_page.py` 재작성 — 내부에 `subtabs = QTabWidget()` 두고 `[0] 분석 = NirsVotTab`, `[1] 전체 데이터 = FullTraceTab` 추가. `nirs_tab` 속성은 alias 로 유지해 app.py 의 wiring 영향 없음. 검증: 페이지 sub-tab 2개 ✓, HYPER 로드 후 FullTraceTab 콤보=['HYPER'] 자동, refresh 시 axes patches 4개(2 NIRS_VOT + 2 Ex 영역) + 1 line, NOR 추가 후 ['NOR','HYPER'], Close 후 콤보 비움.
- 2026-05-01 v0.6.10.1 FullTraceTab 빈 화면 버그 픽스. 사용자 피드백 — "[전체 데이터] 탭으로 전환해도 그래프가 안 뜸". 원인: invisible 탭 상태에서 measurement_loaded 시그널을 받아 refresh() 호출 → matplotlib constrained_layout 이 axes 사이즈를 거의 0 으로 설정 → 사용자가 탭 전환해도 cached layout 그대로라 빈 화면처럼 보임. (1) `FullTraceTab.showEvent` 오버라이드 추가 — 위젯이 visible 로 전환될 때마다 `self.refresh()` 강제 호출 (실제 위젯 크기 기준으로 다시 그림). (2) `NirsAnalysisPage.subtabs.currentChanged` → `_on_subtab_changed(idx)` 핸들러 추가, idx==1 (전체 데이터) 일 때 `full_trace_tab.refresh()`. 두 경로 다 보강. (3) PNG 저장 검증 (`/tmp/full_trace_test.png`, 92KB) — HYPER 90.8분 9991 samples 의 SmO2 곡선, Baseline 파랑·Recovery2 주황·Ex1/Ex2 빨강 음영, 한글 타이틀 모두 정상 렌더링 확인.
- 2026-05-01 v0.6.10.2 FullTraceTab 빈 화면 — invisible draw 차단. v0.6.10.1 의 showEvent 추가로도 사용자 화면에서 여전히 빈 그래프. 진단: showEvent 가 호출되어 refresh() 가 한 번 더 일어나도, 이미 invisible 시점에 그려진 figure 의 layout cache 가 굳어 있어 visible 전환 후에도 0 사이즈 axes 그대로. 핵심 해결: **invisible 일 때는 draw 자체를 안 하기**. (1) `FullTraceTab._on_measurement_loaded` 가 콤보 동기화만 하고 `if self.isVisible(): self.refresh()` 가드. (2) `refresh()` 시작 부분 `if not self.isVisible(): return` 가드 — measurement_loaded 외 경로(예: 토글 체크박스 클릭) 도 invisible 이면 차단. (3) `_sync_combo_from_main` 에서 invisible 일 때는 `condition_combo` 의 currentTextChanged 시그널을 blockSignals 로 차단해 stale draw 방지. visible 전환은 showEvent 또는 subtabs.currentChanged 에서 자연스럽게 첫 draw. 검증: 분석 탭 활성 상태에서 HYPER 로드 → axes count=0 (draw skip 확인), [전체 데이터] 탭 전환 → axes 1개, 94%×86% layout, lines=1, patches=4, 한글 타이틀 ✓. NOR 추가 후 콤보=['NOR','HYPER'], 정상.
- 2026-05-01 v0.6.10.3 FullTraceTab 음영 안 그려지던 ModuleNotFoundError 픽스 (Windows). 사용자 traceback: `from nirs_vot.metrics import _parse_clock` → `ModuleNotFoundError: No module named 'nirs_vot'`. 원인: WSL 에서는 main.py 가 `sys.path.insert(0, str(Path(__file__).resolve().parents[1]))` 로 `scripts/` 를 path 에 넣지만, Windows 의 일부 실행 경로에서는 그 절대 import 가 ImportError. 다른 모듈은 try/except 로 패키지·스크립트 양방향 import 패턴이지만 full_trace_tab.py 는 inline 으로 직접 import 했음. (1) `from nirs_vot.metrics import _parse_clock` 두 곳 호출 모두 제거. (2) 자체 `_to_ts(anchor_date, time_value)` helper 신설 — `datetime.time` / `'HH:MM:SS'` / `'HH:MM'` 모든 입력 형태를 받아 anchor_date 의 date/tz 를 빌려 tz-aware Timestamp 생성. 외부 모듈 의존 없음. (3) NIRS_VOT 음영 (Baseline 파랑·Recovery2 주황) + 운동 구간 음영 (Ex1·Ex2 빨강) 모두 새 helper 통해 axvspan. 검증: 4 입력 형태 동일 timestamp ✓, patches=4 (NIRS_VOT 2 + Ex 2), legend 5개 라벨, 음영 토글 OFF 시 patches 2개로 줄어듦 ✓. Windows 에서도 import 에러 없을 것.
- 2026-05-01 v0.6.11 FullTraceTab 음영 강화 + Apply/Drag 자동 갱신. (1) **3단계 분할 음영** — 한 NIRS_VOT 세션을 단일 색 axvspan 1개 → 3개 axvspan 으로 분할: `start~inflate` 옅은 회색 (Baseline 측정 1분), `inflate~deflate` 세션 색상 진하게 (Occlusion 5분, Baseline=파랑/Recovery2=주황), `deflate~end` 옅은 초록 (Reperfusion 3분). 각 세션 4 시점(start/inflate/deflate/end) 에 세로 점선 + 라벨. patches 4→8 (2세션×3단계+운동 2), lines 1→9 (Smoothed+8 vlines). 사용자가 "Baseline 음영 맞는지" 의문 → 표준 NIRS-VOT 프로토콜 (1분 baseline+5분 occlusion+3분 reperfusion) 그대로 시각화 확인. (2) **Apply / 그래프 드래그 시 음영 자동 갱신** — `app._on_params_applied` 끝에 `self.nirs_page.full_trace_tab.refresh()` 추가, `_on_anchor_dragged` 끝에도 동일 추가. invisible 일 때는 자체 가드로 skip 되므로 안전. ParameterPanel input 수정 + Apply → 음영 위치 즉시 이동, [분석] 탭에서 vline/marker/baseline 드래그 release → 음영도 새 anchor 기준으로 갱신.
- 2026-05-01 v0.6.12 우측 패널 범용화 + gradient descent Auto Detect + Legend Min/Peak. (1) **dock 타이틀 범용화** — `app._sync_panels` 의 dock_right 제목 "NIRS-VOT · Ours vs ground truth" → "Analysis Results". `ParameterTable` 의 title QLabel 도 동일. 향후 다른 결과 추가 가능. (2) **신규 `nirs_vot/metrics.find_extremum_by_gradient(df, t_start, t_end, kind, lr=1.0, max_iter=300, h_sec=0.5, tol=1e-3)`** — argmin/argmax 그리드 위치를 초기점으로 잡고 finite difference gradient 를 따라 local extremum 정밀 검출. kind='min' 은 descent, 'max'는 ascent. 5초 smoothed 데이터에서 sub-second 정밀도 (~0.5s). (3) **`ParameterTable` 에 Auto Detect 섹션** — QGroupBox "Auto detect Min / Peak · gradient descent" + "▶ Detect" 버튼 + Min·Peak 결과 라벨 (빨강/초록 색상). 시그널 `auto_detect_requested` emit. analyze 후 enabled, clear 시 disabled + 라벨 리셋. (4) **`app._on_auto_detect_requested`** — anchor 의 inflate~deflate 에서 min, deflate~end 에서 peak 검출 → `lf.overrides[sess]['min_time'/'peak_time']` 적용 → analyze_session + full_trace_tab.refresh. ParameterTable 결과 라벨에 표시. (5) **Legend 에 Min/Peak 추가** — NirsVotTab._update_render 에서 marker artist 의 set_label() 갱신 + ax.legend() 자동 모음. legend texts: ['Raw', 'Smoothed (5s)', 'Baseline 62.4%', 'Min 9.7%', 'Peak 81.1%', 'Oxygen Deficit', 'Hyperemia (AUC 3min)']. (6) draggable_overlay `_to_xfloat` 의 nanosecond 경고 정리 (Timestamp.replace(nanosecond=0)). 검증: GD min 9.667% @ 14:05:59.497, grid argmin 동일 9.667% @ 14:05:59 (sub-second 정밀); Auto Detect 시그널 → override 적용 + 재분석; legend 7항목 포함.
- 2026-05-01 v0.6.13 ParameterPanel 에 Min/Peak 그룹 추가 (수치 + 시각 편집). (1) 신규 QGroupBox "Min / Peak (현재 분석 결과)" — Min row (`time` QTimeEdit + `value` QLabel 빨강 굵게) / Peak row (QTimeEdit + 초록 굵게) / Magnitude row (Peak−Min 자동 계산). 시각은 사용자가 편집 가능, 값은 보간 자동 표시. (2) `ParameterPanel.show_for(cfg, last_result=None)` 시그니처 확장 + `_update_minpeak_from_result(r)` 헬퍼 — MetricsResult 의 min_time/min_smo2/peak_time/peak_smo2/magnitude 값을 input/label 에 채움. `_last_result` 멤버로 보존해 Reset 시 재사용. (3) `collect_overrides()` 메서드 — `min_time`/`peak_time` 의 사용자 입력값을 datetime.time dict 로. 0:0:0 sentinel 은 dict 미포함 (자동 검출 모드 복귀 의도). (4) `app._on_params_applied` 에 Min/Peak override 적용 — collect_overrides 결과를 anchor_date 와 결합해 tz-aware Timestamp 로 lf.overrides[sess] 에 commit, 또는 sentinel 시 키 제거. (5) `app._on_nirs_result_ready` / `_refresh_panels_for` / `_on_anchor_dragged` 가 panel.show_for(cfg, last_result=...) 호출 — 분석 결과·드래그 후·Auto Detect 후 모두 panel Min/Peak 그룹 자동 갱신. 검증: HYPER 분석 → panel "14:05:59 / 9.67%" 표시, magnitude "71.44% (Peak−Min)"; Min time 14:04:00 직접 입력 + Apply → 28.89% 보간 + 재분석; Auto Detect 후 panel 자동 14:05:59 복원; Reset → last_result 로; Close → 모든 라벨 "— %".
- 2026-05-01 v0.6.14 HRV 외부 분석 결과 카드 — 값 표시 보류 (자체 엔진 미구현). 사용자 피드백 — "구현되기 전까지 외부값을 카드에 표시하면 우리가 분석한 결과로 오해할 수 있으니 — 로 두자". `widgets/external_hrv_card.ExternalHrvCard.show_for(condition, cfg)` 가 이전엔 `cfg.hrv_truth[tp]` 의 SDNN/RMSSD/LF/HF/LF·HF 5개 값을 셀에 set_value 했지만, 모두 None 으로 변경 (= "—" 표시). context_label 도 "HRV 분석 엔진 미구현 — 값 표시 보류" 로. note 텍스트도 "분석 엔진이 추가되면 자동으로 채워집니다" 안내로 갱신. UI 레이아웃·QGroupBox·_MetricCell 구조는 그대로 — 자체 엔진 붙으면 set_value 만 다시 활성화하면 즉시 동작. `cfg.hrv_truth` 데이터 자체는 보존 (config_loader 그대로) — 추후 분석 검증 시 ground truth 비교에 재사용 가능. 검증: HYPER 로드 후 HRV 페이지 진입 → context "HRV 분석 엔진 미구현 — 값 표시 보류", Baseline·Post 모두 SDNN=—/RMSSD=—/LF=—/HF=—/LF/HF=— ✓; cfg.hrv_truth.Baseline = {SDNN: 28.3, RMSSD: 19.6, ...} 데이터 보존 ✓.
- 2026-05-01 v0.6.15 SGD multi-start + Robust extremum 검출 (사용자 "GD 가 진짜 min 못 찾는 에러" 픽스). 사용자 보고: 일부 데이터에서 단순 GD 가 local optimum 에 갇혀 진짜 minimum 을 못 찾음. (1) 신규 `find_extremum_by_sgd(df, t_start, t_end, kind, n_starts=12, max_iter=200, lr=1.5, noise_sigma=0.6, seed=42)` — 확률적 경사 하강법 multi-start: ① 구간 내 균등 분포 random 초기점 12개, ② 각 step 마다 Gaussian noise (decay) 주입 → simulated annealing 유사 탈출, ③ lr decay (1.5 → 0.75), ④ 모든 시작점 결과 중 best (kind='min' 은 최소, kind='max'는 최대) + 마지막에 grid argmin/argmax 안전망. seed 고정으로 재현성. (2) 신규 `find_extremum_robust(df, t_start, t_end, kind)` — GD 와 SGD 모두 호출, 더 좋은 쪽 (min: 작은 / max: 큰) 반환 + method 라벨 ("GD"/"SGD"). 단일 알고리즘 결과를 결코 능가당하지 않음을 보장. (3) `app._on_auto_detect_requested` 가 robust 함수 호출. status 라벨에 어떤 method 가 채택되었는지 표시 (`Auto detect: min=24.33% @ 12:20:21 [SGD], peak=… [GD]`). 검증: HYPER occlusion (smooth 곡선) GD=SGD=9.6667 동일 → robust 가 GD 채택 (재현성); HYPO occlusion **GD=25.1111 vs SGD=24.3333** (0.78% 차이, SGD 가 진짜 global min) → robust SGD 채택 ✓; SGD seed=None 5회 시행 모두 9.6667 일치 (수렴 재현성).
- 2026-05-01 v0.6.16 검출 구간 확장 + Differential Evolution global optimizer + Legend 위치 외부. 사용자 보고 — "HYPO Calf 의 진짜 min 은 12:20:39 (deflate 이후 9초)". 검증: HYPO 의 SmO2_smooth 그리드 argmin = **23.5556 @ 12:20:39.5** — occlusion 구간(12:15:30-12:20:30) 안에선 24.33 가 한계, 진짜 global min 은 deflation 직후의 dip 에 있음. v0.6.15 의 SGD 가 24.33 만 잡았던 이유 = 검출 구간이 occlusion 으로 제한되어서. (1) **검출 구간 확장**: app._on_auto_detect_requested 가 min 검출 시 inflate~deflate 가 아닌 inflate~deflate+30s 까지 검색 → deflation 직후 dip 포함. (2) **신규 `find_extremum_by_de(df, t_start, t_end, kind, maxiter=200, tol=1e-7, seed=42)`** — scipy.optimize.differential_evolution 기반 global optimizer. popsize=20, mutation=(0.5,1.5), recombination=0.85, polish=True (마지막에 L-BFGS-B). 1D quasi-convex 가정 없는 진짜 global 검색. (3) **`find_extremum_robust` 가 GD+SGD+DE 모두 시도** + 우선순위 (DE > SGD > GD). 검증: HYPO 검출구간 확장 후 GD=25.88 (잘못된 위치), SGD=23.5556@12:20:39.5 (사용자 지적과 정확 일치), DE=23.5556@12:20:46 → robust 가 SGD 채택 (시각이 더 정확). HYPER 도 검출구간 확장으로 GD=8.8889@14:06:02 (deflate +2s, 이전 9.667 보다 더 낮음) 발견. (4) **Legend 위치 외부**: `ax.legend(loc='lower center', bbox_to_anchor=(0.5,1.10), ncol=7)` — axes 영역 위쪽 바깥에 한 줄로 7개 항목. ax.set_title 은 pad=2.0 으로 axes 안쪽 상단. 그래프 데이터 가림 없음. PNG 검증: HYPO 그래프에 Min 마커가 정확히 12:20:39 에 표시.
- 2026-05-01 v0.6.17 분석 그래프 줌/팬 기능. NirsVotTab 의 SmO2 그래프에 인터랙티브 zoom 추가. DraggableOverlay 의 좌클릭 드래그(핸들 이동)와 충돌 회피하기 위해 입력 분배: (1) **마우스 휠** — `mpl_connect("scroll_event", _on_scroll)`. base=1.25 비율로 커서 위치 중심 zoom in (휠 위)/out (휠 아래). 현재 xlim/ylim 의 cursor 상대 위치(`relx`, `rely`)를 보존해 zoom anchor 가 마우스 위치. (2) **우클릭 드래그 박스** — `matplotlib.widgets.RectangleSelector(button=[3], minspanx=5, minspany=5)` 로 좌클릭은 보존, 우클릭만 캡처. props 에 노란 점선 박스 (#FBBF24/#F59E0B) 시각 피드백. onselect → `ax.set_xlim/ylim(box)`. (3) **더블클릭 또는 'r'/'R'/'home' 키 → reset** — `ax.relim() + autoscale_view()` 로 데이터 전체 범위 복원. (4) info_label 안내 갱신: "│ 줌: 마우스 휠 │ 박스 줌: 우클릭 드래그 │ 리셋: 더블클릭 또는 R 키". 검증: 휠 zoom in → xlim width ×0.8 정확, 박스 줌 → 박스 영역 정확 매핑, 더블클릭 → relim/autoscale_view 호출, RectangleSelector button=[3] 등록 ✓.
- 2026-05-01 v0.6.17.1 줌 후 그래프 "뭉개짐" 픽스. 사용자 보고 — "기능 추가하니 그래프가 뭉개짐". 진단: ① constrained_layout 이 axes 외부 legend (bbox_to_anchor=(0.5, 1.10)) 와 충돌해 매 zoom/draw 마다 axes width 자동 조정 (예: 0.94 → 0.51 절반 축소), ② `fig.autofmt_xdate()` 가 hardcoded `subplots_adjust(bottom=0.2)` 호출로 우리 manual margin 덮어씀, ③ `ax.relim() + autoscale_view()` 가 axvline·marker 의 default datalim (-1, 1) 까지 union 해 xlim 이 (-1007, 21150) 같은 거대한 값으로 잡혀 데이터가 화면 끝에 압축. (1) `_setup_axes` 에서 `fig.set_layout_engine(None)` + `fig.subplots_adjust(left=0.07, right=0.985, top=0.85, bottom=0.16)` manual margin 으로 axes 영역 고정. (2) `fig.autofmt_xdate()` 제거 → 직접 `xticklabels rotation=25, ha='right'` 로 회전 (subplots_adjust 보존). (3) `_update_render` 에서 `relim+autoscale_view` 대신 명시적 `ax.set_xlim(seg.dt[0], seg.dt[-1])` + ylim (raw·smooth min/max ±5% margin) 으로 정확 데이터 범위. (4) `_reset_zoom` 도 `last_result.anchors_abs[s/e]` + raw/smooth ydata 의 min/max 로 명시적 복원. (5) `RectangleSelector useblit=False` (manual layout 과 호환성). 검증: 박스 줌 후 ax position (0.07~0.985, 0.16~0.85) 변하지 않음 ✓; xlim 12:14:30~12:23:30 정확; 박스 줌 후 reset → 초기 xlim 복원 ✓; 그래프 데이터·음영·핸들 모두 정상 표시.
- 2026-05-01 v0.6.18 Zoom Reset 버튼 + Qt 박스형 GraphLegend. (1) **Zoom Reset 버튼** — NirsVotTab toolbar 우측 (Close 옆) "⟲ Zoom Reset" 버튼 추가. clicked → `_reset_zoom`. 단축키 (더블클릭/R 키) 와 동일 동작이지만 명시적 버튼 UI. tooltip 으로 단축키 안내. (2) **신규 `widgets/graph_legend.py` (`GraphLegend`)** — matplotlib `ax.legend` 외부 위치 시 발생한 layout 충돌 회피 위해 Qt 위젯으로 대체. `_LegendChip` (color frame + label) 을 `QGridLayout` 에 박스 그리드로 배치. `set_items([(color, label), ...], n_cols=2)` 로 동적 갱신, `clear_items()` 로 placeholder 복귀. `#FAFBFC` 옅은 배경 + `#E1E4E8` border + 4px radius. (3) **NirsVotTab 통합** — toolbar 다음 info_label 다음에 GraphLegend 위젯 배치 (close 버튼 밑, graph 위). `_update_render` 에서 ax.legend 호출 제거 + `self.graph_legend.set_items(...)` 호출. 항목: Raw / Smoothed (5s) / Baseline %% / Min %% / Peak %% / Oxygen Deficit / Hyperemia (AUC 3min) — 7개 2 columns × 4 rows. ax.set_title `pad` 제거 (legend 가 외부 Qt 위젯이라 axes 안쪽 상단 그대로 OK). `clear_all` 시 GraphLegend.clear_items() 호출. 검증: zoom_reset_btn 클릭 → xlim 초기 복원 ✓; 분석 후 legend 7 cells (Row0 Raw/Smoothed, Row1 Baseline/Min, Row2 Peak/OxyDeficit, Row3 Hyperemia) ✓; clear_all → placeholder cell 1개 ✓; 그래프 자체에는 ax.legend 없음 (Qt 위젯이 대체).
- 2026-05-01 v0.6.19 Legend 를 그래프 안 작은 박스로 (Qt 위젯 제거). 사용자 피드백 — "Qt 그리드 컴포넌트로 빼지 말고 하나의 작은 사각형으로 close 밑에". (1) `widgets/graph_legend.py` Qt 위젯 root layout 에서 제거 (파일은 보존, 미사용). (2) `_update_render` 에서 다시 matplotlib `ax.legend(loc='upper right', ncol=2, fontsize=7, framealpha=0.88, handletextpad=0.3, columnspacing=0.8, labelspacing=0.3, borderpad=0.4)` 호출 — axes 우상단 안쪽 작은 compact 박스. close 버튼 바로 밑 위치 (graph 캔버스 우상단 = toolbar close/zoom_reset 의 바로 아래). axes 안쪽이라 v0.6.17.1 의 외부 legend 와 달리 constrained_layout 충돌 없음. (3) `clear_all` 의 `graph_legend.clear_items()` 호출 제거. 검증: ax.legend loc=1 (upper right), ncol=2, 7 항목 라벨 ['Raw','Smoothed (5s)','Baseline 62.4%','Min 9.7%','Peak 81.1%','Oxygen Deficit','Hyperemia (AUC 3min)'] ✓; Zoom Reset 정상; ax position 그대로 (0.07~0.985 폭 변동 없음). PNG 캡처에 toolbar close 버튼 바로 아래 그래프 우상단 작은 legend 박스 확인.
- 2026-05-01 v0.6.20 Legend 를 figure 우상단 axes 외부로 (사용자 이미지 의도). v0.6.19 의 axes 안쪽 legend 가 reperfusion peak 영역과 살짝 겹쳐 데이터 가림. 사용자 스크린샷의 의도는 "axes 영역 밖 + figure 우상단 (close 버튼 바로 밑) 작은 박스". (1) `ax.legend(...)` → **`fig.legend(handles, labels, loc='upper right', bbox_to_anchor=(0.985, 0.985), ncol=2, fontsize=7, ...)`** — figure 좌표계 사용. axes 위쪽 manual margin (subplots_adjust top=0.85 의 빈 15% 영역) 안에 자동 배치. axes layout 과 분리되어 zoom 시에도 axes position 불변. (2) `self._fig_legend` 멤버로 보존 — update 마다 옛 legend `.remove()` 후 새로 생성 (재분석 시 누수 방지). (3) `clear_all` 에서 `_fig_legend = None` (figure.clear() 가 알아서 정리). 검증: `ax.get_legend() = None` (axes 안 legend 없음), `_fig_legend` 활성, ax position (0.07~0.985, 0.16~0.85) zoom in/reset 모두 불변; PNG 캡처에 close 버튼 바로 아래 figure 우상단 작은 legend 박스 — 사용자 의도 이미지와 일치.
- 2026-05-01 v0.6.21 Apply/연쇄 갱신 흐름 누락 픽스 (사용자 "Apply 가 반영 안 됨" 보고 진단). 시뮬레이션 추적 결과 Apply 흐름 자체는 정상이나, **`session_combo.currentTextChanged` 시그널 wiring 누락** 발견 — 사용자가 Baseline↔Recovery2 전환해도 panel/table 갱신 안 됨. 또 last_result 가 다른 session 의 결과인 채로 panel min/peak 표시되어 "Apply 후에도 옛 결과" 처럼 보이는 케이스. (1) `nv.session_combo.currentTextChanged` → `_on_session_changed` wiring 추가, `_refresh_panels_for(condition)` 호출. (2) `_refresh_panels_for` 가 `last_result.session` 도 검증 — condition 또는 session 이 콤보 값과 다르면 last_result=None 으로 panel min/peak 비움 (잘못된 결과 표시 방지). (3) `_on_params_applied` 끝에 `panel.show_for(cfg, last_result)` 명시적 호출 안전망 추가 (result_ready 가 이미 처리하지만 중복 OK). 종합 검증: A) Baseline 분석 (9.67/81.11) → Recovery2 콤보 → panel —/— ✓ → Recovery2 분석 (5.89/81.78) → Baseline 복귀 → —/— (재분석 필요 표시) ✓; B) HYPER → NOR 콤보 → —/— → NOR 분석 (12.78/69.11) ✓; C) lead 30s + Baseline.start 14:01:30 변경 + Apply → dynamic_anchors=(14:01:00, 14:01:30, 14:06:30, 14:09:30) 정확 반영, panel input 보존, min_value 9.67% 갱신, param_table 12 rows.
- 2026-05-01 v0.6.21.1 Windows 옛 .pyc 캐시 자동 정리. 사용자 traceback `from nirs_vot.metrics import _parse_clock ModuleNotFoundError: No module named 'nirs_vot' (line 222)` — 실제 코드는 v0.6.10.3 에서 이미 `_parse_clock` import 제거됐는데, Windows `.venv-win` 의 옛 `.pyc` 가 그대로 남아 import 시 옛 코드 실행. (1) `run.bat` 시작 시 `for /d /r "scripts" %%d in (__pycache__) do rmdir /s /q "%%d"` 로 옛 캐시 자동 삭제. (2) `set PYTHONDONTWRITEBYTECODE=1` 환경변수 추가 — 이후 .pyc 새로 만들지 않아 영구 stale 방지. (3) **신규 `clean.bat`** — 긴급 청소용. `__pycache__` 디렉터리 + scripts/*.pyc 모두 삭제. (4) Apply 한 번만 작동 보고 → 시뮬레이션 4번 연속 Apply (lead 30→60→Min 14:04→Wattmax 400+lead 45) 모두 cfg/dynamic_anchors/last_result 정확 갱신 확인 → 코드 자체 OK, 옛 `.pyc` 가 옛 `_on_apply` 흐름 실행했을 가능성 진단. 캐시 정리로 해결 예상.
- 2026-05-01 v0.6.22 Apply 시 input 강제 commit (포커스 이슈 안전망). 사용자 보고 — "Apply 문제 여전, 포커싱 문제인가?". 진단: QSpinBox / QDoubleSpinBox / QTimeEdit / QDateEdit 는 `keyboardTracking` 옵션에 따라 사용자가 lineEdit 에 타이핑 후 Enter / focus-out 시점에서야 텍스트를 `value()` 에 반영. 사용자가 input 에 새 값 타이핑 후 곧장 Apply 버튼을 마우스로 누를 때 platform / Qt version / focus 이벤트 순서에 따라 옛 value 로 collect 될 가능성. (1) `ParameterPanel._on_apply` 시작에 `_commit_all_inputs()` 호출 추가 — 모든 spin/time/date input 의 `interpretText()` 강제 호출로 lineEdit 텍스트를 즉시 value() 에 반영. (2) lead_spin/tail_spin/min_time_edit/peak_time_edit/NIRS_VOT 시각 4개/HRV 시각 4개/Subject info 의 int/float/date 필드 모두 포함. QLineEdit/QComboBox 는 항상 즉시 반영이라 제외. 검증: lineEdit.setText('45') 직접 시뮬레이션 → before lead_spin.value()=60, typing 후 45, apply 후 45 commit → dynamic_anchors.start=14:00:15 정확; lineEdit.setText('14:01:45') → cfg.timepoints.NIRS_VOT.Baseline.start=14:01:45 적용. 추가로 시간 시각 input (`HH:mm:ss` 포맷) 도 동일 처리.
- 2026-05-01 v0.6.23 HRV timepoint 영구 disabled + Apply 시각 피드백. 사용자 피드백 — "HRV timepoint 는 분석에 안 쓰니 수정 안 되는 게 맞지 않나? 풀어둔 이유는? 그리고 Apply 시 시각 피드백 필요". (1) **HRV timepoint disable** — `widgets/parameter_panel.py` 의 `_hrv_inputs[sess]['start'/'end']` 4개 모두 `setEnabled(False)` + `setReadOnly(True)` 영구 적용. group title 도 "HRV timepoint (참고용)" → "HRV timepoint (분석 엔진 미구현 — 표시 전용)" 로 명확화. `set_enabled_inputs(enabled)` 가 NIRS_VOT 만 토글하고 HRV 는 건들지 않게 수정 (옛 코드는 데이터 없을 때 disable, 있을 때 enable 했음). (2) **Apply 시각 피드백** — `_on_apply` 끝에 `_flash_apply_feedback()` 호출. 버튼 텍스트 `▶ Apply` → `✓ Applied`, 배경색 `#0969DA` (파랑) → `#16A34A` (초록). `QTimer.singleShot(900, self._restore_apply_btn)` 으로 0.9초 후 자동 원상복귀. 사용자 클릭 액션 명확 인지. 검증: HRV 시각 4개 모두 enabled=False/readOnly=True ✓; NIRS_VOT 시각은 enabled=True ✓; Apply 클릭 직후 버튼 "✓ Applied" + 초록 배경 ✓; 0.9s 후 원상복귀 ✓; clear_all 후에도 HRV 영구 disabled.
- 2026-05-01 v0.6.24 Auto Detect 버튼을 ParameterPanel 로 이동 (Result 는 표기만). 사용자 피드백 — "Auto detect Min/Peak 버튼은 Session Parameters 의 Min/Peak 그룹에 있는 게 맞다, Result 는 값 표기만". (1) `ParameterPanel` 의 Min/Peak QGroupBox 안에 "▶ Auto detect by gradient descent" 버튼 추가 (Magnitude 행 아래, columnspan=3). 신규 `auto_detect_requested` 시그널. tooltip 으로 "GD + SGD + DE 알고리즘 검출 → override 적용 + 재분석" 안내. `set_enabled_inputs(enabled)` 에 `auto_detect_btn.setEnabled(enabled)` 추가 (data 있을 때만 enabled). (2) `ParameterTable` 의 Auto Detect 섹션 (QGroupBox + auto_status / auto_min_label / auto_peak_label / auto_detect_btn / auto_detect_requested 시그널 / set_auto_detect_result 메서드) 모두 제거. 단순 비교 테이블 + Export 버튼만. (3) `app._wire_signals` 에서 `param_table.auto_detect_requested` → `param_panel.auto_detect_requested` 로 변경. `_on_auto_detect_requested` 안의 `param_table.set_auto_detect_result(...)` 호출 제거 (panel 의 min_value_label / peak_value_label 이 result_ready 콜백에서 자동 갱신). 검증: panel.auto_detect_btn 존재 + 텍스트 "▶ Auto detect by gradient descent" ✓; table 의 auto_detect_btn / auto_min_label / auto_detect_requested 모두 None (제거됨) ✓; HYPO 분석 후 panel.auto_detect_btn 클릭 → panel min "23.56 % @ 12:20:39" 자동 갱신 (사용자 지적 정밀 위치) + lf.overrides 적용 + 재분석 ✓.
- 2026-05-01 v0.6.25 Toggle 도크 액션 TypeError 픽스 + close 동기화. 사용자 보고 — "Toggle Sessions Browser / Toggle Results Panel 기능 작동 안 함" + traceback `TypeError: QDockWidget.setVisible() takes exactly one argument (0 given)`. 진단: `act.triggered.connect(dock.setVisible)` 직접 연결 시, PySide6 의 `triggered` 시그널이 `()` / `(bool)` 두 오버로드 중 ambiguous → no-arg 오버로드와 매칭되어 `setVisible()` 인자 0 개로 호출됨 → TypeError 로 동작 실패. (1) **`triggered.connect(dock.setVisible)` → `triggered.connect(lambda checked: dock.setVisible(checked))`** — lambda 로 인자 명시. `act_toggle_browser` 와 `act_toggle_results` 둘 다 적용. (2) **`dock.visibilityChanged` → `act.setChecked` 동기화 추가** — 도크의 X 버튼으로 닫혀도 toolbar/메뉴 액션의 checked 상태 자동 동기화 (이전엔 stale 한 채로 남아 다음 클릭이 무반응처럼 보였음). (3) tooltip 명확화: "해제 시 그래프 영역 확장". 검증: trigger 2회 (visible→hidden→visible) 정상; dock.close() (X 시뮬레이션) 후 action.checked=False 자동 동기화 → 다음 trigger 로 visible 복귀 정상. dock_params 는 v0.6.18 에서 이미 lambda + visibilityChanged 동기화 적용되어 무관.
- 2026-05-01 v0.6.26 Windows 단일 .exe 빌드 스크립트 (`build.bat`). PyInstaller 기반. 사용자 더블클릭 → 자동 PyInstaller 설치 (없으면) → `--onefile --windowed` 로 단일 `dist\physical_analysis.exe` 생성. 옵션: `--paths scripts` 로 import path 확장, `--add-data "scripts/nirs_vot_gui/assets/icons;..."` 로 SVG 아이콘 동봉, hidden-import 명시 (scipy.optimize._differentialevolution / scipy.optimize._lbfgsb_py / scipy.special / openpyxl / openpyxl.cell._writer / matplotlib.backends.backend_qtagg / nirs_vot.* / nirs_vot_gui.* / config_loader), `--collect-data PySide6 matplotlib` + `--collect-submodules matplotlib scipy`. 빌드 전 build/dist/spec/.pyc 모두 자동 정리. 빌드 후 사이즈 표시. 사용자 흐름: setup.bat (1회) → build.bat (수 분) → dist\physical_analysis.exe 단일 파일 → 다른 PC 로 복사·더블클릭 실행 가능. 데이터(참고자료/) 는 사용자가 [Open folder] 로 선택. 첫 실행 시 onefile 압축 해제로 수 초 소요는 정상.


- 2026-05-09 v0.6.27 ParameterPanel 파라미터 스냅샷 Save/Load (xlsx/csv/txt 3종). 사용자 요청 — "현재 존재하는 파라미터 기준으로 excel/csv/txt load·save 템플릿 + 기능". (1) 신규 `widgets/parameter_io.py` — 단일 스키마 (section, key, value) 트리플 시퀀스를 3 형식 모두로 round-trip. SUBJECT_KEYS 19 + NIRS_VOT/HRV 시각 4×2 + Anchor lead/tail + Override (baseline_enabled/value/min_time/peak_time) = 32 키. xlsx 는 openpyxl 로 4컬럼 (Section/Key/Value/Hint) bold 헤더 + 섹션 그룹화, csv 는 utf-8-sig (Excel 한글 호환), txt 는 INI 스타일 (`# `/`;` 코멘트, `[Section]` 헤더, `key = value ; hint`). (2) `serialize(panel)` 가 `_commit_all_inputs()` 후 `collect_subject_info/nirs_windows/hrv_windows` + spinbox/checkbox/timeedit 직접 읽어 트리플 생성. (3) `apply_to_panel(panel, state)` 가 부분 로드 허용 (빠진 키는 기존 값 유지) + 알 수 없는 (section, key) 는 warning 으로 수집 (crash 안 함) + 형식 파싱 실패도 warning 으로. (4) ParameterPanel 헤더에 `📥 Load params…` + `💾 Save params…` 버튼 추가 (기존 `📂 Load preset…` 옆). Load preset 은 원본 4-시트 xlsx 용으로 보존, 신규 두 버튼은 평문 파라미터 스냅샷 전용. QFileDialog 필터 `Excel (*.xlsx);;CSV (*.csv);;Text/INI (*.txt)`. (5) 템플릿 3종 `docs/templates/parameters-template.{xlsx,csv,txt}` 생성 — `pio.template_state()` 가 모든 키를 빈 값(또는 default 60/180) 으로 채운 시퀀스 반환. (6) Round-trip 검증: QApplication offscreen 으로 패널 인스턴스화 → 임의 값 (Name/Age/Gender/W/kg=4.1234/D.O.B/시각 4×2/lead 45/tail 200/baseline_override true 67.42/min·peak 시각) 채우고 32 키 직렬화 → xlsx/csv/txt 각각 저장 → 새 패널에 load + apply → 다시 직렬화 → 32 키 모두 완전 일치 (warnings=0). 부분 로드 검증: 5 키 + 미지 [Unknown].foo 1 키 → applied=5, warnings=1, 파일에 없던 tail_sec 은 기존 default 180 보존, 기존 "기존이름" 은 "새이름" 으로 덮어쓰기 정상.
- 2026-05-10 v0.7.0 NIRS_VOT timepoint 동적화 (사용자가 자유 추가/삭제 + per-timepoint lead/tail). 사용자 요청 — "timepoint 를 여러개로 설정하고 anchor lead/tail 부분도 여러개로". 기존 (Baseline, Recovery2) 2개 고정 + 전역 lead/tail 1쌍 → 사용자가 row 단위로 자유 추가하는 모델로 전환. (1) **데이터 모델**: `config_loader.py` 에 `Timepoint(name, start, end, lead, tail)` dataclass 신설. `SessionConfig.timepoints['NIRS_VOT']` 가 `dict[str, TimeWindow]` → `list[Timepoint]` 로 변경. accessor `nirs_timepoints()` / `nirs_timepoint(name)` 추가. 기존 `nirs_vot_window(name)` 도 backward-compat 으로 유지 (TimeWindow 반환). HRV/exercise 는 dict 그대로. (2) **xlsx 파서 양방향 호환**: `_parse_timepoint` 가 `_detect_long_table` 로 신/구 형식 자동 감지. 구버전 R0~R3 grid (NIRS_VOT 2칸 + HRV 2칸) → 2-row list, default lead=60/tail=180. 신버전 long-table (`Name | Start | End | Lead | Tail` 헤더) → 임의 개수 list. 실제 xlsx (`이찬민_HYPER.xlsx`) 그대로 파싱 PASS. (3) **anchor 산출 동적화**: `nirs_vot/anchors.py` 의 `anchors_from_session_config` 가 `cfg.nirs_timepoints()` 순회 + per-timepoint lead/tail 로 4점 anchor 산출. dict fallback 도 보존. Recovery5 추가(lead=90, tail=240) 시나리오 검증: start=15:28:30 자동 산출. (4) **ParameterPanel UI 동적 행**: `_TimepointRow(QWidget)` 신설 — name/start/end/lead/tail/✕ 한 줄 컴포넌트. `_nirs_rows: list` 로 추적, `+ Add timepoint` 버튼으로 추가, ✕ 클릭으로 삭제. 기존 `lead_spin`/`tail_spin` 전역 SpinBox + `Anchor lead/tail` GroupBox 제거 (각 row 안으로 흡수). HRV 박스는 고정 2칸(display 전용) 유지. `collect_nirs_timepoints() -> list[Timepoint]`, `get_nirs_row(name)` (drag 핸들러용), `_add/_clear_nirs_rows` 내부 메서드. (5) **app.py vline drag 핸들러 동적화**: `_handle_vline_drag` 가 `cfg.timepoints['NIRS_VOT']` 를 list 로 처리, `panel.lead_spin/tail_spin` 직접 참조 제거 → `panel.get_nirs_row(session).lead_spin` 으로 row-scoped 갱신. drag 시 list 의 해당 인덱스 Timepoint 를 immutable replace 후 anchor 재산출. (6) **시각화 5개 touchpoint 동적화**: ① `nirs_vot_tab.py._sync_session_combo()` — condition 변경/measurement_loaded/Apply 후 cfg.nirs_timepoints() 이름들로 session_combo 자동 채움. fallback (Baseline, Recovery2). ② `data_browser.py` 의 `SESSION_NAMES` 제거, cfg 에서 동적 추출 + fallback 상수만 유지. Apply 후 set_loaded 로 트리 전체 rebuild (추가/삭제 반영). ③ `full_trace_tab.py` 의 [(Baseline, 파랑), (Recovery2, 주황)] 하드코딩 제거, `_SESSION_PALETTE` (8색 자동 순환) 로 timepoint 마다 음영 색 자동 부여. ④ `parameter_table.py` 의 `_SESSION_TO_TIMEPOINT` 매칭 정책 명문화 — Baseline→VOT_Baseline / Recovery2→VOT_Post (구버전 호환), 그 외 사용자 추가 timepoint 는 GT 빈 값 표시. ⑤ HRV 카드는 별도 도메인이라 변경 없음. (7) **parameter_io.py 직렬화 개선**: 옛 4-키 형식 (Baseline.start/end + Recovery2.start/end + Anchor.lead_sec/tail_sec) → 신 indexed (count + N×{name,start,end,lead,tail}). serialize 가 `panel.collect_nirs_timepoints()` 사용. apply_to_panel 은 신/구 양방향 자동 감지 → 신형식 우선, 없으면 옛 4-키 → 2 row list 로 변환 (legacy Anchor.lead/tail 적용). 검증: Round-trip (3 timepoints 포함 Recovery5 lead=90/tail=240) 완전 보존, 옛 4-키 입력 → 2 row + lead 70/tail 200 정상 적용. (8) 검증 시나리오: 실제 xlsx 로드 → 2 timepoints (구버전 호환) → ParameterPanel 에 2 row 표시 → Add row 로 Recovery5 추가 → Apply → anchors_from_session_config 가 3개 anchor 반환 (Baseline·Recovery2 default lead/tail, Recovery5 lead=90/tail=240) → session_combo 자동 갱신 [Baseline, Recovery2, Recovery5] → 좌측 트리에도 Recovery5 노드 등장 → 전체 데이터 탭에서 음영 3색 자동 부여. 모든 GUI module import + ParameterPanel 인스턴스 생성 + show_for / collect_anchors / round-trip serialize 검증 PASS.
- 2026-05-10 v2 설계 단계 시작 (코드 X, 문서 + 스켈레톤 only). scripts/v2/ 폴더 신설 (app.py / pages/ / widgets/ / theme/, NotImplementedError placeholder) — v1 GUI 와 별도로 진행하면서 분석 엔진 (nirs_vot/hrv_analysis/config_loader) 은 import 재사용. docs/v2/ 4문서 작성: (01) v1 회고 — 도크 토글 모델/show_for 폭발적 호출/하드코딩 timepoint 등 v0.6.x 누적 함정 + 유지할 강점 7개. (02) 기술 스택 비교 — PySide6/Tkinter/Electron/Tauri/FastAPI+React/Streamlit 6 후보 매트릭스, 단일 .exe + 오프라인 + 분석 엔진 Python 재사용 제약 하에 PySide6 유지 권장 + Tauri 차선. (03) 디자인 레퍼런스 보드 — Kubios/Origin/Resolve/Linear/Figma/Notion/Plotly/Grafana 패턴 추출, 사용자가 추가 관찰 채울 자리. (04) README — 결정 트래커 (C-1~C-5 핵심, S-1~S-5 부수) 와 roadmap (설계 → 결정 → 디자인 → 구현). 다음 단계: 사용자가 회고/스택 문서의 빈 영역 채우면 wireframe 단계 진행.
- 2026-05-10 v2 Phase 1+2 구현 — 대시보드 + 카드 + 분석 5종 + 메인 윈도우 + 실데이터 로드 검증. (1) **theme/** — tokens.py (Dark/Light, 형광 그린 accent, 4-step grid, 폰트 토큰), styles.py (전역 QSS 생성). (2) **cards/** — AnalysisCard Protocol + CardMeta dataclass + @register 데코레이터 + AnalysisContext (subject/condition/timepoint/anchor/overrides/vot_truth/all_loaded). (3) **widgets/** — CardFrame (헤더+⋮메뉴+본문 슬롯), KpiCard (큰 값+델타+sparkline custom paint). (4) **dashboard.py** — 12-column QGridLayout, _find_free_slot 자동 placement, set_context 가 모든 카드 재계산, ⋮ 메뉴로 카드 제거. (5) **analysis/** 6 카드 자동 등록 (autoload pkgutil): context (subject 메타) / baseline_smo2 (KPI + multi-cond sparkline) / min_peak (Min/Peak/Magnitude 3개 묶음) / slope_auc (Slope1·Slope2·T50·AUC 4개 카드) / trace (matplotlib 시계열 + 4점 anchor 음영, dark 테마 적용) / comparison (multi-condition × 7 metrics 표). 각 카드는 v1 의 nirs_vot.metrics.compute_metrics 와 anchors_from_session_config 를 그대로 호출. (6) **app.py** — 좌 icon-bar (📊 ⊞ 📥 nav + 📂 load + ⚙) + 상단 context bar (Subject · Condition combo · Timepoint combo · ⊕ Add card · ▶ Analyze) + 메인 dashboard. ⊕ Add card → QMenu 카테고리별 팔레트. condition 콤보 변경 → cfg.nirs_timepoints() 로 timepoint 콤보 동적 채움 → 컨텍스트 자동 전파. (7) **main.py + __main__.py** — `python -m v2` 진입점, sys.path 보정, v1 의 한글 폰트 처리 재사용, 전역 QSS 적용. (8) **run_v2.bat** — Windows 개발 모드 실행 (v1 setup.bat 만든 .venv-win 재사용). 검증: offscreen Qt 부팅 → 기본 레이아웃 6 카드 배치 (context 4×2 + trace 8×4 + baseline 4×2 + min_peak 4×2 + slope_auc 4×2 + comparison 12×4) → 참고자료/NIRS_VOT 폴더 로드 → 3 condition (NOR·HYPO·HYPER) 자동 페어링 → context combo 동적 채움 → baseline_smo2 compute 결과 v1 과 일치 (NOR Baseline 68.52%, GT 68.52%, Δ −0.00%). docs/v2/ README, 05 대시보드 디자인, scripts/v2/ 전체 26 모듈. 다음: Phase 3 (드래그 reorder/리사이즈, 레이아웃 자동 저장, Cmd+K) + Phase 4 (Compare/Export 모드).
- 2026-05-10 v2 디자인 톤 의료/연구 표준으로 재정의 (사용자 피드백 — "검정 칙칙, 대학원·연구실·의료업계 사용"). (1) 기본 테마 Dark → **Light**. bg #0A0C10 → #FFFFFF / #F7F9FB. text #E8EBF1 → #0F172A (진한 slate, 가독성 ↑). accent #4ADE80 (형광 그린) → **#0284C7 (의료 블루 / sky-600)**. accent_2 = #14B8A6 (teal). 다크 테마는 set_theme('dark') 옵션으로 보존. (2) 임상 시맨틱 색 신설 — clinical_normal #16A34A (green) / clinical_warning #D97706 (amber) / clinical_critical #DC2626 (red). (3) 데이터 시각화 10색 팔레트 색맹 친화 재선정 (blue/teal/violet/orange/pink/lime/cyan/yellow/emerald/purple). (4) 타이포 가독성 우선 — kpi 24→26px, title 13→14px. font family fallback 에 Segoe UI / Malgun Gothic 추가. (5) spacing 약간 여유 — card padding 16→18, card_gap 8→12, grid_gap 8→12 (Prism / Stripe 영감). (6) shadow 추가 (Light 톤 elevation) — card subtle / hover / modal. (7) QSS 갱신 — 호버 시 의료 블루 outline, primary 버튼은 채움형 #0284C7, ToolTip 다크 채움. (8) docs/v2/06-의료-연구-디자인-medical-research.md 신규 — GraphPad Prism 10 / JMP / MATLAB / Origin / JASP / R Studio / 3D Slicer / OsiriX / OHIF / Epic Hyperspace / Cerner / Kubios / LabChart / Polar / TrainingPeaks / Stripe / Linear (Light) / Notion / Datadog / Posit 20+ 앱 카테고리별 정리 + 차용 패턴. 색맹 친화 + 인쇄 친화 + 환자 컨텍스트 항상 표시 (EHR 영감) 원칙. 검증: 라이트 테마 윈도우 부팅 OK, 6 카드 모두 새 톤 자동 적용, 실데이터 6 condition 로드 → 분석 결과 v1 일치.
- 2026-05-10 v2 Phase A — v1 의 핵심 편집 기능 + Sessions Browser + 메뉴바 통합. (1) AnalysisContext 에 ``cfg`` (SessionConfig 참조) + ``apply(action, payload)`` 콜백 추가 — 편집 카드들이 mainwindow 의 dispatcher 호출. ACTION_SUBJECT_INFO/TIMEPOINTS/TTE/OVERRIDES/AUTO_DETECT 5 액션. (2) **편집 카드 4종 신설** — SubjectEditorCard (19 필드, v1 의 _INFO_FIELDS 재사용) / TimepointsEditorCard (이름·시각·lead·tail 동적 행, v1 _TimepointRow 패턴) / TTECard (TTE1/TTE2/Maintenance + 70% 임계 경고) / OverridesCard (Baseline lock + Min/Peak 시각 override + Auto detect 버튼). (3) **SessionsBrowser** widget 신설 + 좌측 dock — 헤더 (📂 Open) + 트리 (Subject → condition → timepoint 시각). 더블클릭으로 컨텍스트 전환 시그널. v1 의 data_browser 와 같은 패턴, 라이트 톤. (4) **메뉴바** — File (Open Ctrl+O / Load·Save params Ctrl+S / Export Ctrl+E / Export all Ctrl+Shift+E / Quit) + View (Toggle Sessions Ctrl+B) + Help (About). v1 의 parameter_io.py 그대로 import 해 직렬화 재사용 — panel-less serialize/apply 헬퍼 (_serialize_session_params, _apply_loaded_params) 신설. Export 도 v1 의 ParameterTable 의 정적 메서드 (_write_single_session_xlsx, write_multi_session) 재사용. (5) **Auto detect** — overrides 카드의 ▶ 버튼 → ctx.apply(AUTO_DETECT) → mainwindow 가 compute_metrics 호출해 결과의 min_time/peak_time 을 lf.overrides 에 기록 + 컨텍스트 재전파. (6) **dispatcher 패턴** — 모든 편집 카드 → ctx.apply(action, payload) → mainwindow._apply_dispatcher 분기 → cfg/lf 갱신 + dynamic_anchors 재산출 (timepoint 변경 시) + timepoint 콤보 자동 동기 + Sessions tree rebuild + dashboard.set_context 로 모든 카드 재계산. (7) 검증: 10 카드 등록 (기존 6 + 새 4), 6 condition 로드, 8 카드 compute+render 100% OK, 콜백 시퀀스 (TTE→cfg.exercise, Subject→cfg.subject_info, Timepoints→ cfg.nirs_timepoints + combo 동기) 전부 PASS. 다음 (Phase B): 그래프 위 드래그 핸들 (TraceCard 에 v1 DraggableOverlay 통합) + Full trace card + HRV 카드들.
