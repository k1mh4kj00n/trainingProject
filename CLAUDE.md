# 프로젝트: physical_analysis

> 이 파일은 Claude Code가 새 세션을 열 때 자동으로 읽는 프로젝트 규칙이다.
> `{{...}}` 로 둘러싸인 값은 프로젝트에 맞게 직접 수정한다.

## 프로젝트 메타 (각자 수정)

- 프로젝트명: physical_analysis
- 한 줄 설명: (아이디어 발굴 단계 - 방향/주제 탐색 중)
- 타겟 시장: 한국
- 주 고객: (검증 중 - 아직 미정)
- 현재 단계: 아이디어 발굴/검증

## 응답 언어 규칙 (최우선)

- 모든 응답은 한국어로 작성한다.
- 기술 용어는 한영 병기한다. 예: 린 캔버스(Lean Canvas), JTBD(Jobs to be Done)
- 영어 번역투, em-dash 남발, 영어식 명사 나열을 피한다.
- 자연스러운 한국어 문장을 사용한다.

## 사용자 정보 (각자 수정)

- 역할: 1인 창업 준비 중 (개발자)
- 코딩 경험: 고급
- 사용 언어: 한국어

## 기획 원칙 (모든 작업에 적용)

1. **구체성 우선**: 일반론, 모호한 표현 금지. 모든 분석에 수치, 사례, 출처를 포함한다.
2. **가정과 사실 분리**: 검증되지 않은 추정에는 반드시 `[가정]` 태그를 붙인다.
3. **건설적 도전**: 사용자의 아이디어를 무비판적으로 긍정하지 않는다. 약점과 리스크를 솔직히 지적하되, 극복 방안도 함께 제시한다.
4. **결론 우선(BLUF)**: 결론을 먼저 제시하고 근거를 나중에 설명한다.
5. **로컬 시장 우선**: 타겟 시장 데이터를 먼저 활용하고, 없으면 글로벌 데이터를 사용하되 타겟 시장 적용 시 차이점을 명시한다.

## 파일 저장 규칙

- 모든 기획 산출물은 `./docs/` 폴더에 저장한다.
- 파일명은 한국어-영어 병기 형식: `린캔버스-lean-canvas.md`
- 비용/매출 수치는 원화(₩)로 표시하며, 필요 시 USD를 괄호 안에 병기한다.
- 진행 상황은 `./docs/progress.md`에 기록한다.

## 작업 흐름 (참조용)

1. 아이디어 발굴 → `./docs/아이디어-목록-ideas.md`
2. 아이디어 검증/선정 → `./docs/검증-{날짜}-validation.md` (`/validate-idea`)
3. 린 캔버스 → `./docs/린캔버스-lean-canvas.md` (`/lean-canvas`)
4. 페르소나 → `./docs/페르소나-persona.md` (`/persona`)
5. 경쟁사 분석 → `./docs/경쟁사분석-competitors.md` (`/competitor-analysis`)
6. 시장 조사 → `./docs/시장조사-{주제}-market.md` (서브에이전트 `market-researcher`)
7. PRD → `./docs/mvp-prd.md` (`/prd`)

## 사용 가능한 스킬 (slash command)

- `/lean-canvas` — 린 캔버스 9블록 작성
- `/validate-idea` — 아이디어 7차원 검증 (빌드/피봇/포기 판정)
- `/competitor-analysis` — 경쟁사 5~8개 분석 + 포지셔닝 맵
- `/persona` — 유저 페르소나 3~5명 생성
- `/prd` — MVP PRD 작성 (AI 빌더용 프롬프트 포함)

## 사용 가능한 서브에이전트

- `market-researcher` — 시장 규모·트렌드·규제 심층 조사
- `idea-validator` — 까다로운 VC 시점의 냉정한 아이디어 평가

## 컨텍스트 유지 규칙

- 새 세션 시작 시 `./docs/progress.md`를 먼저 확인한다.
- 작업이 끝나면 `./docs/progress.md`에 한 줄로 진행 상황을 기록한다.
- 주제가 바뀌면 `/clear`로 새 세션을 권유한다.

## 카나리아 (컨텍스트 품질 점검용)

- 모든 응답의 첫 줄에 `🎯` 이모지를 포함한다.
- 이 이모지가 사라지면 사용자에게 `/clear`를 제안한다.
